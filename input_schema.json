{
  "title": "Awwwards Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of Awwwards URLs (e.g. websites, collections) to scrape.",
      "editor": "requestListSources",
      "prefill": [
        { "url": "https://www.awwwards.com/websites/" }
      ]
    },
    "maxItems": {
      "title": "Max Items",
      "type": "integer",
      "description": "Maximum number of items to extract.",
      "default": 100,
      "minimum": 1
    },
    "proxyConfig": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Select proxies to be used.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": [
    "startUrls"
  ]
}